# Lint & test tooling (used by local dev and CI)
pytest>=8.0
pytest-cov>=5.0
ruff>=0.8

# Minimal runtime deps needed to import the modules under test.
# Heavy packages (manim, gradio, dashscope, ...) are NOT required for unit tests;
# install the full runtime deps from requirements.txt for local development.
openai>=1.0
jinja2>=3.1
python-dotenv>=1.0
