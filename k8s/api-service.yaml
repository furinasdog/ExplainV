apiVersion: v1
kind: Service
metadata:
  name: explainv-api
  namespace: explainv
spec:
  selector:
    app: explainv-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP                     # 生产环境可改为 LoadBalancer
